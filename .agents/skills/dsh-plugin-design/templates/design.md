# 主题（范围括注）

状态：<草稿 | 已对齐 | 实施中 | 已实现>（一句话现状；不写演变过程）
范围：这份文档管到哪里。
约束：动手前不可越过的边界（可选）。

## 结论摘要

| 议题 | 结论 | 一句话理由 |
| ---- | ---- | ---------- |

## 事实基线

判断依据的现状；每条给可核查定位（`路径:行号`、命令输出、上游文档链接）。

## 取舍

被否掉的选项，以及否掉的理由。

## 开放问题

还没定的事。定了就删，结论上提到「结论摘要」。

（正文只写当前的事实与取舍：不写「原来怎样 / 迁移自 / 已废弃」——历史由 git 保证。）
