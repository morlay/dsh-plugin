# 系统设计

状态：**已实现**（19 个包 + 示例工作区；示例工作区的 web 与 desktop 两种模式可跑）
范围：仓库的**布局**（包怎么分组与分层）与**装配**（一个工作区怎么把这些包组装成可运行 profile）——包清单与门面见根
[`README.md`](../../README.md)，上下文边界与术语归属见 [`CONTEXT-MAP.md`](../CONTEXT-MAP.md)，记录树的布局见
[`.agents/README.md`](../README.md)。各上下文自己的设计与取舍在自己的层：会话编辑闭环见
[设计 20260917-会话编辑闭环装配](../../packages/session/better-session/.agents/designs/20260917-会话编辑闭环装配.md)，其余在各自的包层 `.agents/`。
约束：**不改上游、不改 vendor**（`@deepseek-ai/*` 与 `vendor/**` 只读，扩展只走 cordis 插件层与本地 patch）。

## 结论摘要

| 议题     | 结论                                                                                                                             | 一句话理由                                                                                                                                    |
| -------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 包分组   | `packages/<family>/<pkg>` 两层：family 只是目录分组，**不等于**上下文分组                                                        | 上下文按词汇边界划分，一个 family 的包可分属不同上下文                                                                                        |
| 记录树   | 两层：根 `.agents/`（跨包跨层事实）、包层 `packages/<family>/<pkg>/.agents/`                                                     | 改动属于哪一层就写那一层，别处链接过去                                                                                                        |
| 装配     | 一个工作区 profile = `dsh.profile.bundles` 顺序应用的一串 bundle patch                                                           | 每个能力包自带 patch，装配顺序写在工作区这一处                                                                                                |
| 装配顺序 | 能力包（插行 / 禁用行）在 `@morlay/dsh-profile` 之前，`@morlay/dsh-session-mode` 在最后                                          | 前者只按行 id 配值，得先有行；后者要按会话收口注入面                                                                                          |
| 用户层   | `profiles/<name>/cordis.patch.yml`（设置面板写的那份）在**每个 bundle 层之后**应用                                               | 装配层给默认值，用户改设置照样生效                                                                                                            |
| 上游     | `vendor/deepseek-harness/` 是按版本克隆的 side workspace，本仓库所有包经 `workspace:*` 解析到它                                  | 上游只读；扩展只走 cordis 插件层与本地 patch                                                                                                  |
| 桌面     | 同一份 profile 由 `desktop/dsh-desktop-host` 跑，`desktop/dsh-desktopify` 只做打包，`desktop/dsh-desktop-shell` 只做 Electron 壳 | 不为桌面另写宿主与启动逻辑；壳与工具分家见 [设计 壳独立成包](../../packages/desktop/dsh-desktop-shell/.agents/designs/20260924-壳独立成包.md) |

## 布局

```
packages/<family>/<pkg>      发布包（19 个，9 个 family：session / context / client / profile / llm / web / subagent / sandbox / desktop）
apps/dsh-custom-next/        示例工作区（web 与 desktop 两种模式，装配声明在这里）
devpackages/devkit/          共享开发配置 @local/devkit（本地私有包，不发布）
vendor/deepseek-harness/     上游 side workspace（只读）
```

family 是目录分组：`session` 放会话编辑上下文的四个包、对话 UI 接管包与「对话管理」面板页包，`context` 放注入能力组与引用展开，而
`profile` 里三个包分属「profile 组成」这一个上下文的两半（配置初始化 / 模式数据 / 工具清单）。哪个上下文归哪些包、
术语表的 home 在哪，见 [`CONTEXT-MAP.md`](../CONTEXT-MAP.md)；包清单与各包门面见根 [`README.md`](../../README.md)。

记录树按同样的层组织：跨包事实（会话编辑词汇、规范、本设计、仓库级决策与债）在根 `.agents/`，包内事实在
`packages/<family>/<pkg>/.agents/`——例如会话编辑闭环的装配设计在
[`packages/session/better-session/.agents/designs/`](../../packages/session/better-session/.agents/designs/20260917-会话编辑闭环装配.md)。

## 装配

装配是**一串 bundle patch**，由工作区的 `dsh.profile.bundles` 声明顺序
（[`apps/dsh-custom-next/package.json`](../../apps/dsh-custom-next/package.json) 的 `dsh.profile.bundles`）：

| bundle                          | 装什么                                                                                                            |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `@morlay/better-session`        | 会话编辑四层：持久化（RDB，替换官方 jsonl）、分支数据、编排服务、对话 UI 槽位替换，以及通用 schema 表单的行配置页 |
| `@morlay/dsh-context-assembler` | 注入通道（`assembler` / `agent-instructions` / `skill-catalog`）与按会话收口                                      |
| `@morlay/dsh-agent-toolkit`     | agent 的工具清单与工具说明（族索引、用法分组）                                                                    |
| `@morlay/dsh-subagent`          | 接管上游 `ctx.subagents` 行（薄壳 fork，只改回报文案）                                                            |
| `@morlay/dsh-sandbox-local`     | 禁用官方沙箱行并插入替换行（`ctx.sandbox` / `ctx.fs`）                                                            |
| `@morlay/dsh-web-search-ollama` | 插入搜索后端注册行（`ctx.web`）                                                                                   |
| `@morlay/dsh-profile`           | profile 的配置初始化：按行 id 配值、关掉部署不要的行，**一行都不插**                                              |
| `@morlay/dsh-session-mode`      | 两个会话模式作为数据（persona / 能力开关 / 角色 / 默认模型）+ 按会话收口的两行                                    |

顺序有判据，不是习惯：能力包的 patch 先插行、`dsh-profile` 只按**行 id** 覆盖 `config`，所以它必须排在那些能力包之后，
否则覆盖找不到行（[`packages/profile/dsh-profile/README.md`](../../packages/profile/dsh-profile/README.md) 的「装配不在这里」）；
`@morlay/dsh-session-mode` 的 `context-assembler-scope` 要在注入通道装配完之后按会话收口。用户在设置面板改的那份
`profiles/<name>/cordis.patch.yml` 在所有 bundle 层之后应用，所以装配给的是默认值。

真装配探针是 `pnpm exec tsx packages/desktop/dsh-desktop-host/tool/verify-session-mode.mts`——它读组合后的真实装配，
profile 层改动的实测判据就是它。桌面模式不改这份装配：`desktop/dsh-desktop-host` 跑同一 profile（无端口传输、不挂
office 技能），`desktop/dsh-desktopify` 只负责把工作区打包 / 运行为 Electron 应用。

## 与上游的关系

只读红线（上游 `@deepseek-ai/*` 不可修改，扩展走 cordis 插件层）见 [`AGENTS.md`](../../AGENTS.md)；上游源码以 side workspace
形式 vendor 到 `vendor/deepseek-harness/`
（[ADR-20260917-上游以side-workspace版本锁定完整代码而非发布版本](../adrs/20260917-上游以side-workspace版本锁定完整代码而非发布版本.md)），
更新与适配流程见 [`dsh-plugin-upstream-sync` 技能](../skills/dsh-plugin-upstream-sync/SKILL.md)。

## 取舍

被否掉的路线（理由的 home 是各自的 ADR / 债务，这里只列仓库级结论）：

- **为桌面化另写宿主与启动逻辑**、不复用上游：会产生第二套后端启动路径
  （[ADR-20260917-自研离线桌面打包器而非直接用上游桌面应用](../adrs/20260917-自研离线桌面打包器而非直接用上游桌面应用.md)）。

## 开放问题

- 本层只放仓库级的事：其余未决事项的 home 不在本文——仓库级债在 [`../debts/`](../debts)，各包与各上下文的未决在设计 /
  债的「开放问题」小节。
