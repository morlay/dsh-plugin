# 临时接管上游对话 UI 的 client 半

状态：未销账（`@morlay/dsh-client-ui-conversation` 是官方 `ui-conversation` 行的**薄壳 fork**、由我们维护；
装配里官方该行 `disabled: true`，insert 我们的一对一行）

**现象**

对话外壳的改造点全在组件内部——粘贴决策与插入路径在编辑器与 InputBar 里、队列行自己算文本——而插件面只有
「整格 shadow keyed slot」，没有 wrap 上游组件的机制。所以这一行由我们接管，**我们是它的发布方**。

装配在 `packages/session/better-session/cordis.patch.yml`：官方 `ui-conversation` 行 `disabled: true` + insert
我们的 `ui-conversation-fork` 行。`disabled` 停掉的是官方那行的装配（槽声明与渲染），它声明的 22 个
target-neutral 槽（`main.conversation` … `conversation.input.model`）由我们的 `client/apply.ts` 同形承担
（`children` 既声明子槽也授权 `renderSlot`）；槽、locale 命名空间与设置命名空间都与上游同名同形。上游已发布包
反过来依赖 fork 的槽面（`ui-subagent` 的 `conversation.content` 工厂、`ui-sidebar` 的
`conversation.session.header.leading`、`ui-agent-preset` 的 `conversation.hero.agentPreset`），fork 少声明一个席
就静默失效。

形态是**薄壳**：只复制我们**有意改过**的文件，其余上游文件不复制——保留文件里指向它们的 import 走相对路径指向
`vendor/deepseek-harness/packages/client/ui-conversation/src/...`，构建时由 tsdown 内联进 `dist/client.cjs`
（发布物自包含，`files: ["dist"]`），开发态由 `dev-client-bundles` 现场打包。包级写法约束（根 `tsconfig.json`
保持 `composite: false`、合并接口只能一份实例）见[本包规范 how-to-write](../standards/how-to-write.md)，测试口径见
[本包规范 how-to-verify](../standards/how-to-verify.md)。

## 保留文件（15 个非测试源 + 7 个守护测试）

同步上游时**只按这张表保留我们的实现**，其余文件不复制、直接引 vendor 源：

| 保留文件（`packages/session/ui-conversation/src/`）                 | 保留什么                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `client/input/reference-text.ts`、`client/queue/queue-text.ts`      | 纯逻辑：引用插入形态归一（`@` mention / `skill:` token）、队列行文本投影                                                                                                                                                                                                                                                                             |
| `client/input/facade.ts`、`client/input/hub.ts`                     | `insertReference` 走纯文本、`restoreDraft` 收窄、scoped 事件归一（`referenceTextOf` / `insertTextOf`）                                                                                                                                                                                                                                               |
| `client/input/editor/runtime.ts`、`client/input/editor/span-map.ts` | chip 插入路径删除、纯文本引用装饰关闭（输入框保留 raw markdown，`@path` / `@dir/` 等高亮不再产生）                                                                                                                                                                                                                                                   |
| `client/queue/QueueDock.tsx` + `QueueDock.styles.ts`                | 队列行按 raw markdown 渲染（`queueRowTextOf` → `<ReferenceMarkdown>`）；编辑 = 撤回到输入框（`remove` 成功后 `restoreDraft` 原文，无确认）——**有意偏离**：上游自带 inline edit，我们不用它（撤回后能在完整输入框里带着上下文改，体验明显更好），同步时不回退这一项；行预览的单行展示由 CSS 把 markdown 块级结构压成 inline 流 + `text-overflow` 裁切 |
| `client/contract/input.ts`                                          | 扩宽：`SessionInput` / `InputActions` / `SessionInputResolver` 带上 `restoreDraft`（extends 上游那份声明）                                                                                                                                                                                                                                           |
| `client/index.ts`、`client/apply.ts`                                | 入口与装配（`Context.input` 收窄、注册 `InputHub` / `queueDockEntry` / `InputBar`）                                                                                                                                                                                                                                                                  |
| `client/skeleton/InputBar.tsx` + `InputBar.styles.ts`               | 样式改 css-in-js、`data-composer-stats` 留白配套、上下文占用按钮跟随上游：`ContextMeter` 在卡片下方 `dock` 里（`composer.dock` 统计席之后），不在输入行 trailing 内（上游位置见 vendor 包 README「Shell 与标准 props」）                                                                                                                             |
| `src/index.ts`（薄壳转出上游 host 入口）、`src/css-modules.d.ts`    | host 半整体转出 vendor；vendor 组件的 `*.module.css` ambient 声明                                                                                                                                                                                                                                                                                    |

保留文件的守护测试 7 个在 `src/__tests__/`（落点与判据见[本包规范 how-to-verify](../standards/how-to-verify.md)）。

**决策点**：`ReferenceInsert.appearance` 的 `| 'skill'` 去掉（全仓库无生产者）；`contract/slots.ts` /
`contract/draft-editor.ts` 不保留——复制会让合并接口出现第二份实例（`TS2717`）。

**影响**

- 这些文件不再随上游升级自动收益：每次同步都要人工判定冲突并保留我们的实现；
- 上游 client 契约换代（如队列面改由 `inbox` 投影承载）时 fork 必须跟随——不跟随不是「不一致」而是功能静默失效；
- 装配缺口（`client/apply.ts` 的 slot 注册、`client/skeleton/*`、`client/service.ts` 无测试）登记在
  [债务 对话UI客户端半的装配面缺测试辅助](../../../ui-conversation-message-actions/.agents/debts/20260917-对话UI客户端半的装配面缺测试辅助.md)。

**触发条件**

- 每次同步上游（`just vendor prepare`）都要处理这些文件的冲突（口径见下），此时是回退评估的时机；
- 上游具备下面四项能力中的任一项时，逐项评估回退（不必等四项齐全）：

1. **编辑器内部的插入决策可插**（上游把插入路径留在编辑器与 InputBar 内部，没有槽）→ 「引用按预定形态归一后
   原样保留」还可改由**引用源**返回 `PickOutcome.text`（官方 `ui-reference` 是唯一产 `insert` 的源）。
   （粘贴改写已完成回退：粘贴只落 `text/plain` 原文，`keymap` / `view-binding` 两个文件随之回退为直接引用 vendor 上游实现。）
2. **composer 草稿模型结构化**：`InputState` 暴露有序的「文本 / 引用 / 附件」块序列（现在仍是 `draft: string` +
   `attachmentIds` + `occurrences`）→ 回退 `restoreDraft` 收窄（改走公开的 `setDraft`）与「插入降级为纯文本」。
3. **组件级替换 / 子区域开槽**（现在只有整格 shadow，样式机制只有上游 CSS Modules）→ 回退队列行渲染改造与输入
   外壳的样式层改造。
4. **队列行按 `content` 渲染——上游已支持**（`QueueDock` 用 `useProjection('inbox')` 并按内容块派生
   `preview` / `text`）→ 先评估这一项，回退后只剩 `ReferenceMarkdown` 的 chip 渲染差异。

**销账条件**

Done when：上面四项里至少一项被上游支持，且对应回退动作执行完（逐项销账，不要求四项齐全）——**第 4 项的「编辑」半除外**：
上游的 inline edit 我们不采用，「编辑 = 撤回该条到输入框」是刻意的体验选择（见保留文件表），哪怕上游已支持也不回退：

- 删掉 `packages/session/better-session/cordis.patch.yml` 里对应的 `disabled: true` 行与 insert 行；
- 删掉 `packages/session/ui-conversation` 及其在 profile 依赖树中的声明；
- 该 fork 期间产出的能力改为经上游扩展面承载。

**核查（2026-09-21）**：四项里第 4 项上游已具备、但**我们不回退**（见下）；其余三项未变。

- 第 4 项（队列行按 `content` 渲染）：上游确实已支持——`QueueDock` 按 `content` 派生（`previewOf` / `textOf`）、
  用 `useProjection('inbox')` 取行，还自带 inline edit
  （`vendor/deepseek-harness/packages/client/ui-conversation/src/client/queue/QueueDock.tsx:20-31,109-110,158-203`）。
  **但这一项不回退**：上游那套的编辑是 in-place inline edit，回退就会丢掉「编辑 = 撤回该条到输入框」的体验
  （[2026-09-21 决定] 撤回后能在完整输入框里带着上下文改，体验明显更好）；行文本投影与 `queueAttachments` 属于
  可选的内部对齐，收益只是让保留面小一点，优先级低。
- 第 1 项（编辑器插入决策可插）、第 2 项（草稿模型结构化）、第 3 项（组件级开槽）仍不具备：
  上游 `ui-conversation` 的插入路径仍在编辑器与 InputBar 内部，`InputState` 仍是 `draft: string` +
  `attachmentIds` + `occurrences`，插件面仍只有整格 shadow。

**同步记录（2026-09-22，上游 0.1.7-alpha.1）**：保留文件表不变（仍是那 15 个非测试源 + 7 个守护测试），
但契约面这轮跟随了五处「落后于上游」：

- **槽面拆了一层**：`main.conversation` 的 child 从 `conversation.session.header` 改成 `conversation.header`
  （新增注册，`ConversationHeader` 组件 + `conversation.header.leading` root 席位）；
  `conversation.session.header` 让出 `leading`，`conversation.composer.bar` 增加 `conversation.input.activity`；
  `conversation.session` 的注入面多一个 `inspectCall`（工具调用定位到 View 的对应位置）。
- **client settings 面换代**：`ctx.settingsScope.bind(...)` → `ctx.configForms.get(...)`（上游 0.1.7 的
  configForms 服务）。
- **InputActions 多了两项**：`captureInsertion()` / `insertText(text, span)`（异步插入一条独立撤销步）。
- **图标命名换代**：`IconXxxOutline{14,16,20}` → `IconXxxOutlineRegular`（尺寸交给 `size` prop，默认值与原数字一致）。
- **消息头部的耗时 / TTFT / tok-s 读数去掉**：上游 0.1.7 不再提供 `message.ranFor` 与
  `message.turnTime.ttft` 这两个 chat 文案，耗时读数收进会话统计弹窗与轮次过程节点；我们的
  `MessageIconActions` 随之只剩时钟与动作（这不是四项回退条件里的任何一项）。

**不修的理由**

上游没有组件级扩展面（见「现象」），不接管就做不了输入框 raw markdown 与队列行渲染的改造；代价只在上游同步时
结算，可以承受。

## 同步口径

- fork 的 `src/` **不是**上游的逐字快照：同步按差异项逐条判定「有意偏离」还是「落后于上游」——有意偏离保留我们的
  实现，落后项跟随上游；
- 与我们的改动冲突时**保留我们自己的实现**，不回退、不迁就上游当前形态；只有「上游已支持」或「我们不再需要偏离」
  才执行上面的回退动作；
- 回写粒度按能力项（上面的四项），不按文件；每次同步注明当时的 `DEEPSEEK_HARNESS_VERSION`（基线见根 `mise.toml`）。

## `turn-tail` 不能承载覆盖（当前结论）

keyed 格子的 `children` 既是子槽声明也是 `renderSlot` 授权：官方 `conversation.chat.node` 的 key `turn-tail`
那一格已声明 `conversation.chat.turnTail` / `conversation.chat.assistant-actions`（消费者 `ui-deliverables` /
`ui-plan` / `ui-message-feedback`）。同 key、同优先级再注册即 fail loud：

```text
slot "conversation.chat.turnTail" is already declared (by an entry in "conversation.chat.node")
```

带 `children` 覆盖撞「already declared」，不带 `children` 覆盖则丢这两个槽的全部贡献。因此这一格只能沿用官方实现；
可用的覆盖面是 list 槽按 id shadow——编排层在 `conversation.composer.dock` 的 id `stats`（priority −1）上覆盖官方
同 id 行，就是这样承载它自己的统计行的。
