# 设计总览

状态：已实现
范围：本包（`packages/session/session-rdb/`）的设计总览——仓库结构、坐标模型与核心
不变量；各主题（表结构 / 写路径 / 读路径 / 分支能力 / 并发写入 / 事件重建 / 旧数据修复）
各有一份同层设计文档，用法见 [README](../../README.md)。

本文件是 `@morlay/session-rdb` 设计文档的**入口**，只写坐标模型与核心不变量；
具体机制（表结构、流程、修复、分支能力）分别落在同层其余设计文档里。

## 仓库结构

本包位于 better-session monorepo 的 `packages/session/session-rdb/`，是三层
cordis plugin 的**实现层**：契约层 `@morlay/session-branch`（provider 抽象）→
本包（实现）→ 编排层 `@morlay/ui-conversation-message-actions`（完整 rewind/retry/fork 功能）。

```
packages/session/session-rdb/
├── cordis.patch.yml        # bundle 声明：dsh plugin 装配本插件
├── .agents/                # 本包记录树（designs/ 设计 + adrs/ 决策，`<YYYYMMDD>-<slug>.md`）
├── src/                    # 实现（entities / adapters / 后端 / storage-takeover / testing …）
├── drizzle/                # 表结构迁移（sqlite / pg 各一份；当前手写，见 debts/20260921-drizzle生成器与实体定义对不上.md）
├── tool/pg/                # PostgreSQL 测试实例（compose + justfile）
└── README.md
```

## 坐标模型（单一稠密坐标）

**核心不变量：写路径零转换（忠实存储原始事件），读取集中转换（有据可查）。**

- `t_session_events.f_sequence` 是**稠密持久化 seq**：事件原样落库（与上游
  JSONL 一致，无过滤无重编号），**连续递增、无空洞**。
- `t_events` 是**全局事件实体**，`f_data` 存**完整事件**（type/seq/time/data/
  ignorable 信封，与 JSONL 每行同构）——忠实存储，读回时整体解析。不含任何
  session 专属信息——一个事件行可被多个会话的桥接行引用（fork 派生会话复用
  父会话事件行，不复制）。
- session 专属信息（稠密 seq、surface 元数据 `surfaceOp`）全部在
  `t_session_events` 桥接行上；`sourceEventSeqs` 不落库，读取时按需重计算。

## 核心不变量

1. **写路径零转换**：事件内容、surfaceOp 原样落库；写路径当前格式校验
   （`validateStoredEvents`）只拒绝未知类型（非 ignorable）与非法消息形状，
   不做坐标转换。
2. **上游 seq 与稠密 seq 恒等**：原样存储下事件 seq 即稠密 seq，写读天然
   对齐——无需坐标映射（见 [写路径](20260917-写路径.md) /
   [读路径](20260917-读路径.md)）。
3. **fork 坐标自洽**：fork 前缀经 `readLog` 读取视图（replace range 已是稠密
   坐标），重编号后子会话坐标与父稠密坐标数值相同——无需额外坐标重映射；
   子会话桥接行 `f_sequence` 是**子会话自己的上游空间**（不复制父值，避免
   两段空间重叠）（见 [分支能力](20260917-分支能力.md)）。
4. **rewind 保留区 range 完整性**：replace range 引用更早事件 ⇒ 截断尾部
   不破坏保留区 range（见 [分支能力](20260917-分支能力.md)）。
5. **SCHEMA_VERSION 门禁**：破坏性表结构变更必须 bump；表结构级升级走 `drizzle/` 目录里的迁移
   （运行时 drizzle `migrate()` 执行）——迁移当前**手写**：drizzle-kit 的生成器输出与本包实体定义对不上
   （[债务 20260921-drizzle生成器与实体定义对不上](../debts/20260921-drizzle生成器与实体定义对不上.md)），
   同版本内数据格式差异（含 surface 语义损坏）在读取/导出视图修复（不落库，
   见 [读路径](20260917-读路径.md) / [旧数据修复](20260917-旧数据修复.md)）。
6. **孤儿事件行由 GC 通道回收**：rewind / torn-tail 产生的无引用事件行不随删除清理；
   `deleteSession` 只落该会话的桥接行 / workspace 归属行 / 投影行 / 会话行，无引用的事件行成为孤儿，
   由 GC 通道回收（[ADR 20260918-删除不再清孤儿与vacuum独立成gc通道](../adrs/20260918-删除不再清孤儿与vacuum独立成gc通道.md)、
   [表结构](20260917-表结构.md)）。
