# 并发写入者检测（多个实例/进程共享同一数据库）

状态：已实现（并发写入 fail loud）
范围：多实例 / 多进程共享同一数据库时的写入者检测；决策见 ADR-同id单写入者与并发写入fail-loud。

本后端的事件**原样落库**（无过滤无重编号，事件 seq 即稠密 seq），而每个
`SessionPersistenceRdb` 实例只在内存里维护自己的 handle cursor。因此两个
后端实例（另一个 `dsh` 进程、或同一进程内重复加载的持久化插件）共享同一
`sessions.sqlite` 时，**同一个 session id 只能有一个写入者**：

- 后端记录每个 session「本实例最后确认的稠密 head」（来自本实例的写入或
  `open` 观察）；`appendBatch` 在事务内校验磁盘 head 与该记录一致。
- 磁盘 head 已被其他实例推进（另一写入者提交过）、或本实例从未读过该 session
  却遇到已有行时，append **fail loud 拒绝**（`modified by another writer` /
  `has a persisted log this instance has not read`），而不是把本批次静默重编号到
  对方尾部——后者会把两组独立 turn 拼接成同一个 log，**事件内容与 seq 语义
  全部错位**（log 级损坏，`UNIQUE(f_session_id, f_sequence)` 无法拦截，因为
  原样 seq 天然无冲突）。
- 不同 session id 的并发写不受影响（各自独立 head），两个实例各写各的 session
  是受支持的多进程部署（`busy_timeout` 只负责让写锁竞争排队）。
- 一个实例 `open`（或 HMR adopt）过某 session 后可以继续 append——那是一次
  明确授权、基于最新磁盘状态的续接；同 id 双实例「都 open 过再各自写」仍不
  支持（需要跨实例协调器，超出本后端职责）。

## 同一介质内的写事务串行

storage 域（工作区 / 归档集 / 投影缓存）的写走 `writeAtomically`：SQLite 侧由
`enqueueSqliteTx` 按数据库路径串行，PG 侧由 `enqueuePgWrite` 按介质身份
（宿主 + 库 + schema，即 `identityBase`）串行。

这不是可选的整洁性，而是 PG 侧的**正确性前提**：事务句柄经实例级字段
（`txOverride`）传给 `db()`，两个并发的 `writeAtomically` 会互相覆盖槽位——
后开始的事务抢走槽位后，先开始的语句落到别人的事务里（一次失败会把另一个
成功事务一起拖垮，PG 契约用例「并发写事务不交错（失败的那个不留痕迹）」覆盖），
或退化成 autocommit。
