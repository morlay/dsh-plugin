{
  "meta": {
    "title": "消息动作",
    "description": "host 侧的 rewind / retry / recall / fork 编排，以及浏览器侧替换 `conversation.chat.node` 的 user / steering 渲染、覆盖 `conversation.composer.dock` 的 stats 行。"
  }
}
