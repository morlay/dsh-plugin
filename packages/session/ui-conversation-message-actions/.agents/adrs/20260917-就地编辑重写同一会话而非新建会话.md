# 就地编辑重写同一会话而非新建会话

状态：已采纳

edit / retry / reroll 采用**就地编辑**语义：rewind 截断到目标轮之前的闭合
`turn/end` 边界，再 append 版本效果与重放输入回**同一会话**（session id
不变、版本树保持单根）；只有 fork 才派生新 id。这是产品语义对齐
[dsh-message-edit](https://github.com/Moeblack/dsh-message-edit) 的决策——
用户期望「编辑这条消息」而不是「开一个新会话」。

## 考虑过的选项

- **每次编辑派生新会话**：实现更简单（纯 append），但会话 id 变化会破坏
  客户端窗口、引用与用户心智模型，与产品语义不符。

## 后果

- rewind 必须支持 live 会话（截断 RDB 与内存 log、对齐 handle cursor、
  重置 agent 轮次游标），是 rdb 实现中最复杂的路径。
- 轮号复用、空轮吸收、assistant manualTurn、客户端投影重置等实现约束，
  以及各操作的完整语义见 [设计 编排层操作语义](../designs/20260917-编排层操作语义.md) 的「就地编辑语义」。
- 版本树因此出现「同 id 多版本」形态：timeline 节点以 `parentSessionId` +
  `seedLength` 血统投影，根为原始会话。

**后续变化（2026-09-20）**：本 ADR 里涉及「版本效果事件」与「版本树（timeline）投影」的部分已被
[ADR-20260920-删除版本树投影并停止写版本效果](../session-branch/.agents/adrs/20260920-删除版本树投影并停止写版本效果.md)
取代——写侧不再产出 `session-branch/version`，读侧投影已删除；其余决定不变。

