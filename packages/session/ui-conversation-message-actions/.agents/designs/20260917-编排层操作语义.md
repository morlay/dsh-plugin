# 编排层操作语义

状态：**已实现**（`ctx.sessionEditor` 的六个动作、HTTP 传输面与浏览器半替换均已落地；版本树查询 `timeline` 已删除，见 [ADR-20260920-删除版本树投影并停止写版本效果](../../../session-branch/.agents/adrs/20260920-删除版本树投影并停止写版本效果.md)）
范围：`packages/session/ui-conversation-message-actions/`——host 侧编排（`SessionEditor`）、传输面与浏览器半替换；
契约面在 `@morlay/session-branch`，数据面在 `@morlay/session-rdb`，装配在 `@morlay/better-session`。

## 服务面（`ctx.sessionEditor`）

| 操作       | 语义                                                                              |
| ---------- | --------------------------------------------------------------------------------- |
| `edit`     | 编辑已落定文本块（用户消息 / 助手块），**就地**（rewind + 重写，session id 不变） |
| `reroll`   | 重生成最后一条已落定助手回复（使用原用户输入）                                    |
| `retry`    | 重试任意历史回合（`truncate` 只重放目标输入 / `preserve` 重放后续全部）           |
| `rewind`   | 截断式回退：原会话回退到闭合 `turn/end` 边界                                      |
| `recall`   | 撤回 user 消息到输入框：只 `rewind` 截断，不重写、不重放（交给用户改后重发）      |
| `fork`     | 从任意闭合边界派生**新会话**（唯一产生新 id 的操作）                              |

传输面一条：宿主 `webServer` 上的 exact 路由 `POST /session-editor`——web 与桌面共用同一张路由表
（无端口形态见
[设计 桌面无端口传输与窗口对齐](../../../../desktop/dsh-desktopify/.agents/designs/20260920-桌面无端口传输与窗口对齐.md)）；
浏览器半恒定请求该路径，不按 `__DSH_TRANSPORT__.ownsHost` 加前缀。

## 就地编辑语义

- **edit / retry / reroll 是就地操作**：`rewind` 截断到目标轮之前的闭合 `turn/end` 边界，再 `append`
  手工回合 / 重放输入回**同一会话**——session id 不变
  （[ADR 就地编辑重写同一会话而非新建会话](../adrs/20260917-就地编辑重写同一会话而非新建会话.md)）；
- **重放复用目标轮号**：轮首 user 编辑（含未闭合轮）整轮截断，重放接回原轮号；编辑 assistant 写入 manualTurn 后同步
  agent 的轮次游标；
- **空轮吸收**：目标轮之前的空轮（`turn/start` 后直接 `turn/end`，可能存在于历史数据中的形状）随截断一并删除，
  重放按保留前缀续号——轮次导航不再出现点不开的空项与轮号空洞；manualTurn 的轮号同样按保留前缀收敛（正常会话等于
  目标轮号）；
- **只有 `fork` 创建新 id**（`ForkOperation` / `forkFrom`，纯 append 派生）；
- **`recall` 只截断不重写**：轮内消息沿用 `edit` 的轮首规则（轮首整轮截断到前一轮 `turn/end`，无前轮则清空全部；
  轮内 followup exclusive drop），轮外 user 消息（`location` 无轮次归属的排队落定消息）按消息自身位置 exclusive drop
  （截断该消息及其后全部），但只 `rewind` 即返回——不重放；被撤回的 user 文本由浏览器半回填到 composer，
  用户修改后自行发送；
- 操作历史不再留痕：`session-branch/version` 事件（版本效果）**已停止落库**，类型定义只作为历史形状保留在
  `@morlay/session-branch`（见 [ADR-删除版本树投影并停止写版本效果](../../../session-branch/.agents/adrs/20260920-删除版本树投影并停止写版本效果.md)）；
- 重放输入经 agent 驱动（见下）排队到原会话，agent 基于截断后历史回复。

## agent 驱动（重放排队输入）

`agents` 服务以 duck-typed 接口使用（不硬依赖 `@deepseek-ai/dsh-agent`），缺失时退化为「已 durable 的就地版本」
（可随时 resume 续跑），见 [ADR 重放经agent驱动而非直接append回复](../adrs/20260917-重放经agent驱动而非直接append回复.md)：

- **live agent**（会话驻留 / 已恢复）：直接 `followup` 排队，不重建、不换 id；
- **rewind 前必须停止运行中的 loop**：每次 rewind（edit / retry / reroll / recall / HTTP `rewind`）都先对驻留 agent
  `cancel({ kind: "user" }, { keepInbox: true })` 再 `whenIdle()`，然后才截断——截断会重写 agent 的 session 内存 log，
  不能与运行中的 loop 并发；`keepInbox` 表示停止本身不丢弃待处理输入，残留排队输入由 rewind 在截断后强制 durable 取消
  （session-rdb 的 live 会话钩子 `inbox.clear`）；实现无 `cancel` 能力时退化为等待其自然停下
  （[ADR rewind前主动停止运行中的loop](../adrs/20260917-rewind前主动停止运行中的loop.md)）；
- **cold 会话**：`resume` 已持久化会话（`create` 对已持久化日志必失败），resume 后 agent 驻留（不 dispose，避免 session
  被移出 store 破坏客户端窗口）；
- 模型 provider / model 在 **rewind 之前**从 `request/header` 解析（就地编辑可能截断掉 header，编辑第一轮 boundary = −1
  时尤甚）。

## 浏览器半（client bundle）

`dist/client.cjs`（单文件，运行期按 `/plugins/??<pkg>/client.js` 服务）经 `__ModuleLoader__.load` 手递，替换
`conversation.chat.node` 的 `user` / `steering` 渲染：

- **shadow 注册**：`user` / `steering` 两个 key 以 `priority: -1` 重新注册（最低优先级渲染，shadow 上游默认注册）；
  其余 key 沿用上游渲染器；
- **composer 统计行覆盖**（`client/composer-stats/`）：`conversation.composer.dock` 的 `stats` id 以 `priority: -1`
  覆盖官方同 id 行（list 槽按 id shadow），渲染搬自上游的统计行（`StatsPills` + `stat-dialog` + `token-format` +
  `turn-reading`），文案沿用官方 `chat` 命名空间字典。根节点保持上游那份的 **shrink-wrap** 形态（只有
  `gap: 12px`，不撑满整行、不带自己的左右留白）：这一行里还有同为 dock 子项的上下文占用按钮，行内间距由
  dock 的 `gap` 统一，统计一旦撑满就会把占用按钮挤到行尾。这一承载面本身结构性安全：分母
  `uncachedInputTokens + cacheReadTokens + cacheWriteTokens` 恒 ≥ `cacheReadTokens`，所以 `token-format` 的越界守卫是
  防御性的（真正可能越界的是数据面被改写或未来新增调用点，守卫就留在我们能改的这一侧）；为什么不改放 keyed 格
  `turn-tail`，见
  [债务 临时接管上游对话UI的client半](../../../ui-conversation/.agents/debts/20260917-临时接管上游对话UI的client半.md)
  的「`turn-tail` 不能承载覆盖」；
- **编辑 / 重试按钮只挂在 user 消息**（`UserMessageNodeView`）：编辑只要消息存在可编辑文本块就显示（不要求轮次归属
  ——`location` 无 turn/step 的已落定消息同样可撤回），点击后不打开编辑弹窗，而是确认（`Modal` + `Button`）后调用
  `recall`——服务端只 `rewind` 截断，客户端把消息文本 `setDraft` 回填到主输入框（`conversation.input`），交给用户修改
  后自行发送；重试仍先弹确认，且只对已闭合轮次开放
  （[ADR 编辑入口不依赖轮次归属](../adrs/20260917-编辑入口不依赖轮次归属.md)）；
- **操作后刷新**：就地编辑后优先调用客户端会话级 `resync()`（重置窗口并重新拉取历史，不整页重载——rewind 的删除无法经
  append-only 事件流表达，seq 回退只做增量会残留旧节点），随后丢弃该会话的全部投影行（`projections.truncate(-1)`）
  ——投影 store 按 higher-seq-wins 保留 rewind 前的高 seq 旧值，截断后的正确值 seq 更小，永远覆盖不上（轮次导航残留已
  删除的轮次）；不可用时回退 `location.reload()`；
- **构建约束**：client bundle 必须是**单文件**，devkit 的 client 入口插件保持平台 seed 与其余 `@deepseek-ai/*` client
  插件行 external（内联会把别的插件的 `__ModuleLoader__.load` 嵌进来导致 duplicate factory），契约层与第三方依赖全部
  内联，动态 import 合并；CSS Modules 经 lightningcss 内联 + `<style data-plugin>` 注入
  （[ADR 客户端bundle单文件与shadow渲染替换](../adrs/20260917-客户端bundle单文件与shadow渲染替换.md)）。

## 分层边界

- 数据层（`forkFrom` / `rewind` / `readBranchPrefix`）来自 `ctx.sessionBranch`（provider 实现，如
  `@morlay/session-rdb`）；
- 本服务只做**编排**：闭合轮次扫描、派生 seed 组装、rewind 命令透传、agent 驱动、HTTP 面。
